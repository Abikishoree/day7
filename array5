#include<stdio.h>
int main()
{
    int a[n],i,sum=0;
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        sum=sum+a[i];
    }
    for(i=0;i<n;i++)
    {
        printf("%d",sum-a[i]);
        sum=sum-a[i];
    }
    return 0;
}