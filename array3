#include<stdio.h>
int main()
{
    int n;
    int a[n],i,z=0,o=0;
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        if(a[i]==0)
            z++;
        else (a[i]=1);
            o++;
    }
    printf("zc = %d\noc = %d",z,o);
    return 0;
}